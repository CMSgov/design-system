---
title: Headings
order: 21
intro: Headings help to provide a quick, organized way to organize the main sections of a website.
cmsgov:
  figmaNodeId: 6007-9078
core:
  figmaNodeId: 887-14614
  githubLink: design-system/src/styles/_base.scss
  storybookLink: foundations-typography-headings--all-headings
healthcare:
  figmaNodeId: 2070-49066
medicare:
  figmaNodeId: 4052-37058
---

import {
  Alert,
  Badge,
  CloseIconThin,
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableRow,
} from '@cmsgov/design-system';
import loremIpsumGenerator from '../../../src/helpers/loremIpsumGenerator';
import { css } from '@storybook/theming';

## Heading font family

<ThemeContent onlyThemes={['core']}>Core uses Open Sans for all headings.</ThemeContent>

<ThemeContent onlyThemes={['cmsgov']}>CMS.gov uses Lexend for all headings.</ThemeContent>

<ThemeContent onlyThemes={['healthcare']}>
  Healthcare.gov uses Open Sans for all headings.
</ThemeContent>

<ThemeContent onlyThemes={['medicare']}>
  Medicare.gov uses Montserrat for all headings.
</ThemeContent>

The design system doesn't style base HTML typography elements (like `h1`, `h2`, `p`, etc.) unless they're inside an element with the [ds-content class](/foundation/typography/content). Instead, it provides heading classes (available on this page) corresponding to visual sizes. These classes can be applied to any heading elements, irrespective of their semantic heading level (`h1` to `h6`). This approach allows teams to first choose the semantic heading level that make the most sense for the document and its accessibility and then choose a visual heading size class that fits the page's visual hierarchy.

## Responsive typography

`.ds-text-heading-5xl`, `ds-text-heading-4xl`, and `ds-text-heading-3xl` classes are responsive by default.

To apply responsive typography elsewhere, use the [font size utility class](/utilities/font/font-size/#responsive-font-sizes).
Since the base typography margins and line height is measured in `em` units,
they'll automatically adjust as you change the font size.

<TypographyUsageTable
  caption="All / Small breakpoints"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
    { dataKey: 'usage', label: 'Usage' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--sm',
      figmaToken: 'Headings/Small',
    },
    {
      cssClass: 'ds-text-heading--md',
      figmaToken: 'Headings/Medium',
      usage: {
        cmsgov: 'Footer headings',
        healthcare: 'H2 in megamenus',
        medicare: 'Search headers',
      },
    },
    {
      cssClass: 'ds-text-heading--lg',
      figmaToken: 'Headings/Large',
      usage: {
        healthcare: 'H4 - subsection headings',
        medicare: 'Footer menu headers',
      },
    },
    {
      cssClass: 'ds-text-heading--xl',
      figmaToken: 'Headings/XL',
      usage: {
        cmsgov: 'H3 headers in cards',
        healthcare: 'H3 - subsection headings',
        medicare: 'Card headers',
      },
    },
    {
      cssClass: 'ds-text-heading--2xl',
      figmaToken: 'Headings/2xl/2xl',
      usage: {
        cmsgov: 'H3 headers',
        healthcare: 'H2 - section headings',
        medicare: 'Mobile menu drawer header',
      },
    },
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Headings/3xl/3xl - MD-SM',
      usage: {
        cmsgov: 'Search prompt on homepage',
        healthcare: 'H1 for homepage heroes, page headings',
        medicare: 'Internal page headers',
      },
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Headings/4xl/4xl - MD-SM',
      example: {
        medicare: null,
      },
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Headings/5xl/5xl - SM',
      usage: {
        cmsgov: 'H1 on homepage; internal page titles',
        healthcare: 'H1 on Get Coverage; Keep Coverage',
        medicare: 'Learn, Resources headers',
      },
    },
  ]}
/>

<TypographyUsageTable
  caption="Medium breakpoints"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Headings/3xl/3xl - MD-SM',
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Headings/4xl/4xl - MD-SM',
      example: {
        medicare: null,
      },
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Headings/5xl/5xl - MD',
    },
  ]}
/>

<TypographyUsageTable
  caption="Large breakpoints and up"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Headings/3xl/3xl - LG',
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Headings/4xl/4xl - LG',
      example: {
        medicare: null,
      },
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Headings/5xl/5xl - LG',
    },
  ]}
/>

| CSS Class              | 545px and below | 545px to 768px | 768px and above |
| ---------------------- | --------------- | -------------- | --------------- |
| `.ds-text-heading-5xl` | 36px            | 48px           | 60px            |
| `.ds-text-heading-4xl` | 36px            | 36px           | 48px            |
| `.ds-text-heading-3xl` | 30px            | 30px           | 36px            |

<ResponsiveExample
  storyId="foundations-typography-headings--responsive-headings"
  title="Responsive typography example"
/>

## Headings Links

Links nested within elements styled with heading tags, or styled directly as headings, will render as links in the appropriate size and weight. There is no additional CSS class to apply, but Figma contains tokens specific to rendering links. Medium breakpoints are given as an example below.

Please note that in Figma, the color token is separate from the font token and needs to be applied independently.

<TypographyUsageTable
  caption="All / Small breakpoints as links"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--sm',
      figmaToken: 'Links/Headings/Small',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--md',
      figmaToken: 'Links/Headings/Medium',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--lg',
      figmaToken: 'Links/Headings/Large',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--xl',
      figmaToken: 'Links/Headings/XL',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--2xl',
      figmaToken: 'Links/Headings/2xl/2xl',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Links/Headings/3xl/3xl - MD-SM',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Links/Headings/4xl/4xl - MD-SM',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Links/Headings/5xl/5xl - SM',
      example: 'example-link',
    },
  ]}
/>

<TypographyUsageTable
  caption="Medium breakpoints as links"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Links/Headings/3xl/3xl - MD-SM',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Links/Headings/4xl/4xl - MD-SM',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Links/Headings/5xl/5xl - MD',
      example: 'example-link',
    },
  ]}
/>

<TypographyUsageTable
  caption="Large breakpoints and up as links"
  headers={[
    { dataKey: 'cssClass', label: 'CSS class' },
    { dataKey: 'figmaToken', label: 'Figma token' },
    { dataKey: 'example', label: 'Example' },
  ]}
  data={[
    {
      cssClass: 'ds-text-heading--3xl',
      figmaToken: 'Links/Headings/3xl/3xl - LG',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--4xl',
      figmaToken: 'Links/Headings/4xl/4xl - LG',
      example: 'example-link',
    },
    {
      cssClass: 'ds-text-heading--5xl',
      figmaToken: 'Links/Headings/5xl/5xl - LG',
      example: 'example-link',
    },
  ]}
/>

## Specific examples

### On dark background

<EmbeddedExample>
  <div class="ds-base--inverse ds-u-padding--2">
    <h3 class="ds-text-heading--lg">Example heading on dark background</h3>
    <h3 class="ds-text-heading--lg">
      <a href="javascript:void(0);">Example link heading on dark background</a>
    </h3>
  </div>
</EmbeddedExample>

<ThemeContent onlyThemes={['medicare']}>

### Custom heading styles

#### Teal heading

Only used for landing pages and marketing material to apply emphasis to words within a header.

<EmbeddedExample>
  <h3 class="ds-text-heading--lg ds-u-color--primary">Example teal heading</h3>
</EmbeddedExample>

</ThemeContent>

### Eyebrow/Eyelash header

In some cases, it is better to group header elements into a header component. This can be referred to as the Eyelash/Eyebrow header where you have the main header and an optional top header (eyebrow) or bottom header (eyelash) header.

This is an optional header design.

<EmbeddedExample>
  <h3 class="ds-text-heading--md ds-u-margin-bottom--0">Example eyebrow header</h3>
  <h1 class="ds-text-heading--3xl ds-u-margin--0">Example main header</h1>
  <p class="ds-text-body--md ds-u-margin-top--0">Example eyelash header</p>
</EmbeddedExample>

## Code

<Alert variation="warn" heading="First and last children">

The design system heading styles have alternate margins for headings that are the first or last child of their parent element. Headings that are the first child have no top margin, and headings that are last children have no bottom margin. Some examples of what we mean:

```html
<main>
  <h1>I'm the first child of my parent.</h1>
  <h2>I'm not the first child of my parent</h2>
  <div>
    <h2>
      I'm actually the first child of my parent even though I'm not the first heading on the page
    </h2>
    <h2>I'm the last child of my parent</h2>
  </div>
  <h2>I'm also the last child of my parent</h2>
</main>
```

</Alert>

### Style customization

The following CSS variables can be overridden to change the type sizes:

- `--font-size-5xl`
- `--font-size-4xl`
- `--font-size-3xl`
- `--font-size-2xl`
- `--font-size-xl`
- `--font-size-lg`
- `--font-size-md`, uses `--font-size-base`
- `--font-size-sm`

## Guidance

### The `ds-content` class

For pages that are mainly about written content or places where you want to use a standard mapping of heading-level styles to semantic heading levels, consider using the [`ds-content`](/foundation/typography/content/) class.

### Layout dos and don'ts

There should always be a parent/child relationship between headers and content on the page. Every header should have a relationship to something else on the page. This can apply to many different relationships like a header and a form group or a header and list of links. Stray headers or content should be reevaluated.

- Paragraph styles should never be used as a parent to another text element.
- Header elements should never have equal or smaller font size than the paragraph element.
- Not all paragraphs need a header, but all headers need children.
- Every page should have one heading 1 (H1)
- Headings style appearing under H1 should follow a logical order for information design and supporting various assistive technology devices/software.
- Don't use a smaller header font size than the body font size
- Don't use stray headers
