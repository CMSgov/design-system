{
  "name": "cmsds-token-importer",
  "description": "",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "cmsds-token-importer",
    "manifest": "src/manifest.json",
    "main": "cmsds-token-importer.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "yarn --cwd ../../ build themes json && skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.6.0"
  },
  "author": "Forrest Baer <forrest@forrestbaer.com>",
  "dependencies": {
    "@skpm/console": "^0.2.8",
    "@skpm/dialog": "^0.4.2",
    "@skpm/fs": "^0.2.6"
  }
}
