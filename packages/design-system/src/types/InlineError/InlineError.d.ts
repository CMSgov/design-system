import React from 'react';
interface InlineErrorProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
    inversed?: boolean;
}
declare function InlineError({ children, className, id, inversed }: InlineErrorProps): React.ReactElement;
export default InlineError;
