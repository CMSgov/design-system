/*
To compile typescript and generate definition files, uncomment this file and use the --typescript option
{
  "compilerOptions": {
    // Allows `*.example.tsx` files in `docs` to import directly from `src`, baseUrl is required
    "baseUrl": ".",
    "paths": {
      "@src/*": ["src/*"]
    },
  }
}
*/
