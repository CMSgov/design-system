/*
To compile typescript and generate definition files, uncomment this file and set the `typescript` option in `cmsds.config.js`
{
  "compilerOptions": {
    // Allows `*.example.tsx` files in `docs` to import directly from `src`, baseUrl is required
    // Equivalent to `alias` in webpack's resolve config. https://webpack.js.org/configuration/resolve/#resolvealias
    "baseUrl": ".",
    "paths": {
      "@src/*": ["src/*"]
    },
  }
}
*/
